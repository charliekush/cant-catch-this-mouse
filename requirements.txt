lds2d==0.7.0
numpy==2.5.1
opencv-python==5.0.0.93
pyserial==3.5

# Laptop-only: --backend pose / scripts/human_demo.py (app/perception/pose_identity.py).
# Not needed on the UNO Q -- that path falls back to MoveNet over the same TFLite
# runtime the bbox backend already requires.
mediapipe

