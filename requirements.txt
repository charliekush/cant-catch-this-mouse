lds2d==0.7.0
numpy==2.5.1
opencv-python==5.0.0.93
pyserial==3.5
pytest        # unit tests (dev only)

# Laptop-only: --backend pose / scripts/human_demo.py (app/perception/pose_identity.py).
# Currently unwired from app.main (see DEVELOPMENT_LOG.md); kept for when the
# pose backend is re-integrated.
mediapipe
